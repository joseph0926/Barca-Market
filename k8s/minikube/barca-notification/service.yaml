apiVersion: v1
kind: Service
metadata:
  name: barca-notification
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: barca-notification
  ports:
    - name: notification
      port: 4001
      targetPort: 4001
      protocol: TCP
